# clew: regenerated tool output (state.json stays tracked)
.clew/locations.json
.clew/coverage.json

# Local Claude Code settings — never committed
.claude/settings.local.json

.idea

# Maven build output
target/

# Node / pnpm — clew runs on Node; deps are installed, not committed (pnpm-lock.yaml is)
node_modules/
